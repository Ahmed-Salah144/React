import { Container } from '@mui/material';
import BookListingsSection from '../../sections/blog/BookListingsSection';

export default function BookListingsPage() {
  return (
    <Container maxWidth="lg">
      <BookListingsSection />
    </Container>
  );
}